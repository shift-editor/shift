[package]
edition = "2021"
name = "shift-bridge"
version = "0.0.1"
authors = ["Kostya Farber <kostya.farber@gmail.com>"]
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
shift-font = { workspace = true }
shift-slug = { workspace = true }
shift-wire = { workspace = true, features = ["napi"] }
shift-backends = { workspace = true }
shift-workspace = { workspace = true }

napi = { version = "=3.8.6", default-features = false, features = ["napi6", "web_stream"] }
napi-derive = "=3.5.5"
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0.18"
tokio = { version = "1", features = ["sync"] }
tokio-stream = "0.1"

[build-dependencies]
napi-build = "2.1"
