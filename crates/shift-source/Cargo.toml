[package]
name = "shift-source"
version = "0.1.0"
edition = "2024"

[dependencies]
shift-font = { workspace = true }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2"
zip = { version = "2", default-features = false }

[dev-dependencies]
tempfile = "3"
