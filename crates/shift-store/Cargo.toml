[package]
name = "shift-store"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.37.0", features = ["blob", "backup", "limits"] }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
