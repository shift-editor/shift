# Scratchpad for Small Changes
* selection bounds should be per type (e.g bezier bounds is not simply the bbox of its points)
* not using our Vec libraries enough e.g in resize, `const newX = anchorPoint.x + (point.x - anchorPoint.x) * sx;`
